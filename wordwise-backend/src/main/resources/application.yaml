server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wordwise_db
    username: root
    password: gowtham@bit7
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # use create-drop only for first-time testing
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

servlet:
  multipart:
    max-file-size: 5MB
    max-request-size: 5MB

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
